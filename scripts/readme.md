Scripts auxiliares do projeto FarmTech Solutions (Fase 5).

- `run_tests.sh` — executa a suíte pytest da raiz do repositório.
- `execute_notebook.sh` — reexecuta o notebook local de ponta a ponta.
- `estimate_aws_costs.py` — recalcula o custo On-Demand da API nas regiões `sa-east-1` e `us-east-1`.
